</br><h1>Ici, tu peux éditer ta page. Tu peux donc changer ton nom ou ton adresse email.</h1> </br>
<% provide(:title, "Edit") %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:session, url: edit_after_path) do |f| %>

      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>

      <%= f.label :email %>
      <%= f.email_field :email, class: 'form-control' %></br>

      <%= f.submit "Edit", class: "btn btn-primary" %>

    <% end %>
    </br>
    <p>Pour modifier ton mot de passe, contacte l'administrateur.</p>

  </div>
</div>
